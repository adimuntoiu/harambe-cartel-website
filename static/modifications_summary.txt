# Modifications Summary - 2025-12-09

## Map Route Implementation
1.  **Created Route**: Established a new route at `src/routes/map`.
2.  **Iframe Integration**: Implemented `src/routes/map/+page.svelte` to serve an external HTML map using an `iframe`.
3.  **Static Assets**: Moved raw map files (`map.html`, `mapScript.js`, `styles.css`, `teams.json`, `team-icons/`) to `static/map/` to ensure they are served correctly by the application.

## Navigation Updates
Updated the "Map" button in all module components to link to `/map` instead of the anchor `#map`. Files modified:
- `src/modules/Home.svelte`
- `src/modules/About.svelte`
- `src/modules/Sponsors.svelte`
- `src/modules/Members.svelte`
- `src/modules/Events.svelte`
- `src/modules/Results.svelte`
- `src/modules/Gallery.svelte`
- `src/lib/components/Sidebar.svelte`

## Map UI Customization
1.  **Zoom Controls**: Modified `static/map/mapScript.js` to disable the default zoom control and re-add it in the **bottom-left** corner.
2.  **Navigation Bar**: Added a full navigation bar to the top-right of the Map page (`src/routes/map/+page.svelte`).
3.  **Styling**:
    - Applied standard `.btn` styling (round, semi-transparent) to the Map navigation bar to match the rest of the website.
    - Adjusted button opacity to **0.6 (darker)** for better visibility against the map background.
